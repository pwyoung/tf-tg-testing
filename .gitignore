**/*~
**/*#

**/*.iml
**/*.idea

**/*.env

# Terraform
#
# Exclude all .tfvars files, which are likely to contain sentitive data, such as
# password, private keys, and other secrets. These should not be part of version
# control as they are data points which are potentially sensitive and subject
# to change depending on the environment.
#
*.tfvars
#
**/.terraform/*
# .tfstate files
**/terraform.tfstate
*.tfstate.*
#
**/.tfplan
**/tfplan
#
# Crash log files
**/crash.log
#
**/override.tf
**/override.tf.json
**/*_override.tf
**/*_override.tf.json
#
**/.terraformrc
**/terraform.rc
#
# This is good to add since it just tracks the provider used
#**/.terraform.lock.hcl

**/.terragrunt-cache